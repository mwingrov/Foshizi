import {
  CheckboxContainer,
  FormCheckBox,
  FormLabel,
} from "./ChecboxButtonElement";

const CheckboxButton = () => {
  return (
    <CheckboxContainer>
      <FormCheckBox />
      <FormLabel />
    </CheckboxContainer>
  );
};

export default CheckboxButton;
